#include <stdio.h>

int main(){
    #define tam 10
    int i, j, x[tam], y[tam];
    for(i = 0;i < tam;i++){
        scanf("%d", &x[i]);
    }
    for(j = 0;j < tam; j++){
        scanf("%d", &y[i]);
    }

    return 0;
}
